// ==========================================================================
// Ergalics Studio — Block editor entry (Scratch-like Blockly mode)
//
// Phase 0 placeholder; the Blockly workspace + toolbox lands in Phase 1
// (block-code-modes.md §6, §12 Phase 1).
// ==========================================================================

import { EditorPlaceholder } from './EditorPlaceholder';

export function BlockEditor() {
  return <EditorPlaceholder mode="block" />;
}
