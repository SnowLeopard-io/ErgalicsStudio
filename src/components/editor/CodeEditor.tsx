// ==========================================================================
// Ergalics Studio — Code editor entry (Python/R via Monaco + Pyodide/webR)
//
// Phase 0 placeholder; Monaco + Pyodide land in Phase 2 (block-code-modes.md
// §7, §12 Phase 2).
// ==========================================================================

import { EditorPlaceholder } from './EditorPlaceholder';

export function CodeEditor() {
  return <EditorPlaceholder mode="code" />;
}
