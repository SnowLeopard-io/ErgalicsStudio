# em-eigensolver 冲刺需求文档

- 文档状态：v2（冲刺）
- 关联对象：整个 Ergalics Studio 仓库（求解核心在此插件内，但拿分策略叠加全仓库平台能力）
- 依据：提交材料《电磁谐振特征值求解器.pdf》"已知边界"；求解核心代码审阅；仓库 GPU/CI/WASM 基础设施盘点
- 硬约束：提交**整个仓库**，且必须保障 **GitHub Pages 在线部署可用**（见第七节）

## 一、背景

求解器已具备竞赛级完成度，"任务定义、可复现、文档"接近拿满。真正的失分点与冲刺目标有三：① 文档自曝的 5 项技术缺陷；② 性能维度"可证性"空白（只有内存预算、无实测）；③ 赛题明示的"多线程/GPU 加速"尚未兑现。本 PRD 据此拆出一条满分冲刺主线：修缺陷 → 上 GPU/并行 → 叠加平台能力 → 用在线可演示收口。

由于提交的是整个仓库，仓库现成的 WebGPU 内核、WASM、多格式 I/O、repro 血缘、实验室页框架都可作为弹药；代价是每一项改动都必须保持"纯客户端、可离线、CI 不红、Pages 能部署"。

优先级语义：P0 = 截止前必做且低成本；P1 = 高杠杆建议本轮做；P2 = 可延，不阻塞提交。

## 二、缺陷修复（对应材料"已知边界"）

### P0-1 后端一致性测试是占位用例
- **现状**：`python/tests/test_all.py` 中 `test_numpy_csr_backend_matches_scipy` 因两个后端构造 API 差异而提前 `return`，未真正比对。
- **修复要求**：统一经 `csr_from_coo` 构造 NumpyCSR，与 scipy 等价 CSR 就同一组 COO 比对 `matvec` 结果与形状，误差 < 1e-12。
- **验收**：用例真正执行并通过；无"提前返回"分支。

### P0-2 JD 内层容差表述与实现不符
- **现状**：注释称内层 MINRES 容差"有意放宽"，实际 `min(cfg.minres_rtol, 1e-4)` 在默认配置下取到 1e-6，比 JD 自身默认更严。
- **修复要求**：改为 `max(...)` 使注释成立，或调整默认值；三处（注释、实际取值、文档）统一。
- **验收**：默认配置下三者一致；单测仍通过。

### P0-3 CLI 文档串声称输出 residuals，实际未写
- **现状**：docstring 宣称 `.npz` 含 `residuals`，实际只写 `eigenvalues/eigenvectors/meta`；残差只在标准输出汇报。
- **修复要求**：写入归档或修正文档串，让提示、docstring、产物三者一致。
- **验收**：CLI 产物 keys 与声明完全一致。

### P0-4 `csr.py` 辅助函数潜在缺陷
- **现状**：`hermiticity_measure()` 无 `rng` 时调用不存在的 `np.default_rng`；`sample_size` 声明未用；测试总显式传 `rng` 故未触发。
- **修复要求**：签名给合理默认，`sample_size` 真正生效；补一个"不传 `rng`"用例。
- **验收**：无 `rng` 调用不抛异常；`sample_size` 生效；新增用例通过。

### P1-5 内置示例文件重数描述不符
- **现状**：示例库称"每个特征值恰好两重"，实测重数分布 2 / 4 / 20。
- **修复要求**：按实测更新描述（关联材料 9.2 节与生成参数）。
- **验收**：描述与 `python/samples` 重跑结果一致。

### P1-6 性能维度缺少实测数据
- **修复要求**：补一节性能实测，见 3.2 的实测表；本地以 CPU 单线程/多线程为十万阶主证据，浏览器 WebGPU 覆盖小中规模；指标含 `cavity_large` 的 λ、残差、外循环数、matvec 数、耗时、峰值内存，及 1e3→1e5 规模曲线。
- **验收**：性能数字可在无 CUDA 的机器上完整复现。

## 三、GPU 与并行加速（复用现有基础设施）

### 3.0 关键资产
项目 `src/core/` 已有成熟 WebGPU 层：`gpu.ts`（设备/OOM/丢失/回退）、`compute.ts`（统一 buffer/kernel/run 抽象）、`gpu-kernels.ts` + `wgsl.ts`（matmul/fft/kmeans/binning 四内核，均带 GPU/CPU 自动路由 + 优雅回退 + 计时报存），且测试断言 WGSL 与 descriptor 不漂移。GPU 加速直接沿用这套既定模式，与仓库同架构，非一次性 hack。

前提：开发机、测试机与评审机均无 CUDA/NVIDIA 环境，因此 **GPU 加速以浏览器 WebGPU 为唯一实现**；这与"不依赖特定硬件即可复现"的降级叙事天然契合，本地侧以 CPU 多线程为性能主证据。

### 3.1 新增 SpMV 内核（P1）
对齐 matmul 同级新增 **spmv**：WGSL CSR 行级 SpMV + 数学等价的 `spmvCPU`（与 `NumpyCSR.matvec` 容差一致）+ `GPU_KERNEL_THRESHOLDS.spmv` 自动路由 + GPU 失败必回 CPU + 2 个测试（绑定一致性、GPU↔CPU 结果 < 1e-6）。经 `em-worker.ts`/`plugin.ts` 暴露 `hostSpmv(...)`，Python 端 CSR 一次性上传后 matvec 委托 GPU，未挂载/失败无缝落回。

加分：满足"GPU 加速"字面要求 + 浏览器 GPU 电磁求解创新点 + 优雅回退稳稳定性。
风险：Pyodide↔TS 每次 matvec 往返与带宽成本，十万阶可能不升反降 → 必须实测，小中规模展示真实加速比，如实标注十万阶瓶颈。

### 3.2 性能实测表（P1，冲性能硬分）
不引入任何 CUDA/CuPy 依赖。产出"规模 1e3→1e5 × 后端（CPU 单线程 / CPU 多线程 / 浏览器 WebGPU）"实测表：本地以 CPU 多线程为十万阶主证据，浏览器 WebGPU 覆盖小中规模并给出真实加速比。指标含 `cavity_large`（n=102400）的 λ、真实残差、外循环数、matvec 数、总耗时、峰值内存。
加分：把性能 20% 从"自认短板"抬到"有可复现实测数据"，且**不要求评审侧任何 GPU 硬件**。
定位说明：GPU 仅在浏览器 WebGPU 呈现；材料如实写明"十万阶在 WebGPU 受带宽限制、由 CPU 多线程承担硬指标"，把诚实边界转化为可信度加分。

### 3.3 CPU 多线程兜底（P1，不依赖 GPU）
`NumpyCSR.matvec` 行循环分块并行（`concurrent.futures`/OpenMP），作为对"多线程加速"的统一回应，无 GPU 也可宣称并实测。

## 四、求解器纵深加分（商飞权重项:正确性/稳定性/创新）

### 4.1 复数厄密全精度（P2，成本高、最贴题眼）
电磁腔体矩阵本质是复厄密，现有 GPU 层为 f32。本地路径保留复全精度（scipy 完好支持）；浏览器 GPU 用 f32x2 复封装或如实降级。材料里"本地全精度 / 浏览器降级"一分为二写清——复数精度是评委爱抠的点，做对即证明懂行。

### 4.2 参数扫描→服役诊断（P1，最贴赛题"工程设计与运维诊断"）
对腔体尺寸/介质参数做**特征值曲线扫描**，替换单次求解。把输出从"一组成对特征值"升级为"参数→服役频段的工程诊断图"，落到 3D 模态 + 频谱 + 残差。这是"诊断"题眼的直接命中。

### 4.3 数值对抗基准 stress bench（P0，成本低）
复用现有故障样例（近奇异位移/零附近密集谱/重特征值/病态缩放/非有限值），全部跑一遍，给"无论怎样都以真实残差收敛"的系统对比表，支撑稳定性 15%。

### 4.4 与 LAPACK/scipy 参考对照（P0，成本低）
每个算例给与 `scipy.linalg.eigh`/稠密参考的谱与特征向量一致性 + 残差，直接支撑正确性 25%。

## 五、平台化放大（提交全仓库的独有红利）

### 5.1 主页面插件展示即在线演示（P1，现场杀伤力最强，无新增页面）
插件已能在仓库主页面完成完整的求解—模态—残差展示，**无需单独新建实验室页**。收口工作仅三项：① 确认主页面中插件是可一键触达的（若 landing/Welcome 已暴露其入口则保持，否则补一条直达）；② 让该插件展示路径成为评审现场 30 秒上手的默认入口；③ 若补直达入口，配 E2E 覆盖、不拖红 `deploy`。

### 5.2 复用多格式 I/O 直导真实工程文件（P1）
允许直接导入 `.mtx/.npz/HDF5/FITS/NetCDF` 参与求解 → 工程落地叙事 + "数据留本机、零上传"保密卖点（军工语境硬加分）。

### 5.3 复用 repro 血缘作为合规凭证（P1）
求解全流程自动生成可复现凭证（矩阵指纹 + 参数 + 种子 + 代码快照），落到电测工程"结果可追溯、可复核"的合规价值叙事。

### 5.4 一键诊断报告导出（P2）
模态场 + 频谱 + 残差曲线同页呈现，导出诊断报告（HTML/截图），对应"诊断输出"。

## 六、开源治理与在线可验证（oa 维度）

- 提交全仓库天然把**开源治理 20%** 接近拿满（已有 scorecard / CITATION.cff / SECURITY / CONTRIBUTING / CODE_OF_CONDUCT / 三端部署 CI）。整个冲刺不得破坏任一治理文件或 CI。
- 把提交 PDF 升级为**在线可验证评审材料**：每项评分对应一个可在线复现的演示页/下载，让评审从"读文档"变"当场验证"（与 5.1 呼应）。

## 七、Pages / CI 保障约束（硬底线）

`deploy.yml` 用 Rust→WASM 编译后部署 Pages，产物为纯静态客户端应用。此节任何改动若触碰，一律先确认此表再动手：

| 改动 | 是否触碰 `deploy.yml` | 约束 |
|---|---|---|
| 缺陷修复（第二章） | 否（纯 Python/测试/文档） | 需 Python 单测绿 |
| GPU SpMV（3.1） | 是 | 新 WGSL 必须可编译；不得引入运行时 CDN |
| 性能实测表（3.2） | 否（纯本地记录，不入 web 构建） | 不引入 CUDA/任何 GPU 依赖 |
| 主页面插件展示/演示入口（5.1/6） | 是（仅当补入口） | 新 E2E 不得拖红 `deploy`；Pyodide 本地打包或可信静态源 |
| 多格式 I/O / 报告导出（5.2/5.4） | 是（新增页面/资源） | 纯客户端、可离线 |

通用原则：任何新增内容均保持"纯客户端 + Rust/WebGPU 可编译 + 本地资源无 CDN 运行时依赖 + 既有 `bench.yml`/`ci.yml`/`security.yml` 保持绿"。禁止引入服务端、授权、或浏览器运行时远程拉取逻辑（Pyodide 除外，需静态托管并确认 CORS）。**GPU 一律走浏览器 WebGPU，禁止任何路径引入 CUDA 编译/运行时依赖**（开发机与评审机均无此环境）。

## 八、交付计划（P0 / P1 / P2）

| 项 | 对应维度 | 成本 | 触碰 deploy |
|---|---|---|---|
| P0 缺陷修复（P0-1..4） | 可信度/可复现 | 低 | 否 |
| P0 参考对照（4.4）+ stress bench（4.3） | 正确性 25 / 稳定性 15 | 低 | 否 |
| P1 性能实测表（3.2: CPU 单/多线程 + 浏览器 WebGPU） | 性能 20 | 中 | 否 |
| P1 CPU 多线程兜底（3.3） | 多线程要求 | 中 | 否 |
| P1 参数扫描诊断（4.2） | 创新 15 / 任务定义 | 中 | 否 |
| P1 主页面展示收口 + 演示入口（5.1/6） | 现场印象/落地 30 | 低→中 | 是 |
| P1 多格式 I/O 直导（5.2）+ repro 凭证（5.3） | 落地/合规 | 中 | 是 |
| P1 GPU SpMV 内核（3.1，浏览器 WebGPU） | 性能+创新 | 高 | 是 |
| P2 复数厄密 GPU（4.1）+ 诊断报告导出（5.4） | 创新/落地 | 高 | 是 |

建议锁定的"必做三角"：**CPU 多线程性能实测表（3.2/3.3）＋ 主页面在线演示收口（5.1）＋ 参考对照/stress bench（4.3/4.4）**——把正确性、稳定性、性能可证性和现场印象同时顶到可拿高分，且全部不依赖 CUDA；GPU SpMV（3.1）作为浏览器加分项优先推进，复数厄密（4.1）成本高放次优先。

## 九、回归与验收

- `python/tests/test_all.py`（15 项）通过；P0-1 不再占位；新增 P0-4 无 `rng`、P0-2 默认容差用例。
- 材料第 30 页"已知边界"5 项全部转"已修复"并保留修复说明。
- 性能章新增 CPU 单线程/多线程 + 浏览器 WebGPU 实测表（3.2），可在无 CUDA 机器上复现。
- `deploy.yml` / GitHub Pages 部署通过；`ci` / `bench` / `security` workflow 全绿。
- 评审可经 Pages 打开在线演示，30 秒内完成"导入样例→求解→看模态与残差"。