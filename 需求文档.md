# Ergalics Studio —— 工作站脚手架需求设计文档

> **文档版本**：v3.0 | **状态**：规划阶段
> **定位**：浏览器端专业科学计算工作站工业化脚手架
> **技术栈**：Rust / WebGPU / WASM / 前端框架不限 / 构建工具不限
> **部署目标**：GitHub Pages（纯静态）


## 一、项目定位

### 1.1 核心定义

Ergalics Studio 是一个"浏览器里的科学计算操作系统骨架"。

它是一个空的专业工作站——所有按钮可点、所有面板可开、所有接口可通，但不包含任何具体的科学计算功能。真正的计算能力由第三方插件注入。

### 1.2 设计原则

| 原则 | 说明 |
|:---|:---|
| 接口先行 | 所有扩展点先定义接口，再实现 |
| 框架无关 | 不绑定具体前端框架，开发者自行选择 |
| 国际化原生 | 从第一行代码支持中英双语 |
| 主题原生 | 从第一行代码支持明暗切换 |
| 纯静态部署 | 无后端依赖，全部在 GitHub Pages 上运行 |
| 工业化工程 | 完整的类型系统、CI/CD、错误处理、性能监控 |
| 插件隔离 | 每个插件运行在独立作用域，互不干扰 |
| 数据本地 | 所有数据存储在用户浏览器中，不上传任何服务器 |


## 二、页面与路由结构

### 2.1 路由表

| 路由 | 页面 | 说明 |
|:---|:---|:---|
| `/` | 欢迎页 | 入口页面，硬件检测、语言/主题切换 |
| `/workbench` | 工作台 | 主工作区域，包含侧栏、场景区、参数面板 |
| `/settings` | 设置页 | 系统设置，语言/主题/GPU/存储/关于 |
| `/plugin/:pluginId` | 插件视图 | 插件可注册的独立路由页面 |


## 三、功能模块说明

### 3.1 欢迎页功能

欢迎页是用户访问的第一个页面，承担环境检测和入口引导职责。

**3.1.1 顶栏区域**

| 元素 | 功能说明 |
|:---|:---|
| Logo + 产品名 | 品牌标识，点击回到欢迎页 |
| 语言切换 | 下拉菜单：中文 / English，切换界面语言，偏好存入 localStorage |
| 主题切换 | 下拉菜单：亮色 / 暗色 / 跟随系统，切换界面主题，偏好存入 localStorage |

**3.1.2 主区域**

| 元素 | 功能说明 |
|:---|:---|
| 产品名称 | 大号文字展示 |
| 产品描述 | 小号文字展示定位 |
| 版本号 | 显示当前版本 |
| 进入工作台按钮 | 点击后初始化 GPU 设备，跳转到 /workbench |

**3.1.3 硬件状态区域**

| 检测项 | 检测内容 | 显示状态 |
|:---|:---|:---|
| WebGPU | 检测 `navigator.gpu` 是否存在 | 可用 / 不可用 |
| GPU 名称 | 从适配器信息获取 GPU 型号 | 显示型号或"未知" |
| WASM | 检测 WASM 模块是否加载成功 | 已加载 / 加载失败 |
| 存储 | 检测 IndexedDB 是否可写入 | 可用 / 不可用 |

**3.1.4 底栏区域**

| 元素 | 功能说明 |
|:---|:---|
| GitHub 链接 | 跳转到项目仓库 |
| 文档链接 | 跳转到项目文档 |
| 插件市场链接 | 跳转到插件市场页面 |


### 3.2 工作台功能

工作台是用户进行科学计算工作的主界面。

**3.2.1 顶栏**

| 元素 | 功能说明 |
|:---|:---|
| 菜单按钮 | 控制左侧面板的展开/收起 |
| Logo + 产品名 | 品牌标识，点击回到欢迎页 |
| 当前项目名 | 显示当前打开的项目名称，点击可编辑 |
| 保存按钮 | 保存当前项目到 IndexedDB |
| 分享按钮 | 打开分享对话框 |
| 语言切换 | 同欢迎页 |
| 主题切换 | 同欢迎页 |

**3.2.2 左侧面板**

左侧面板包含项目、插件、工具三个分组。

**项目分组**：

| 功能 | 触发方式 | 系统行为 |
|:---|:---|:---|
| 新建项目 | 点击"新建项目" | 弹出项目名称输入对话框 → 用户输入名称 → 创建空项目 → 顶栏更新项目名 |
| 打开项目 | 点击"打开项目" | 弹出文件选择对话框 → 用户选择 .clproj 文件 → 解析并恢复项目状态 |
| 保存项目 | 点击"保存项目" | 序列化当前项目 → 写入 IndexedDB → 状态栏提示"已保存" |
| 另存为 | 点击"另存为" | 弹出文件名输入 → 生成 .clproj 文件 → 浏览器下载 |
| 最近项目列表 | 自动显示 | 列出 IndexedDB 中最近打开的 10 个项目，点击即可加载 |

**插件分组**：

| 功能 | 触发方式 | 系统行为 |
|:---|:---|:---|
| 加载插件 | 点击"加载插件" | 打开插件加载对话框 |
| 插件列表 | 自动显示 | 显示所有已加载插件的图标和名称，点击切换当前激活插件 |
| 插件状态指示 | 自动显示 | 当前激活的插件高亮显示，未加载的插件显示为灰色 |

**工具分组**：

| 功能 | 触发方式 | 系统行为 |
|:---|:---|:---|
| 设置 | 点击"设置" | 路由跳转到 /settings |

**3.2.3 中央区域**

中央区域是插件的主要工作区。

| 状态 | 显示内容 | 用户可操作 |
|:---|:---|:---|
| 空状态 | 欢迎语 + 三个引导按钮："加载插件" / "打开项目" / "拖拽文件到此处" | 点击引导按钮执行对应操作 |
| 插件已激活 | 插件渲染的内容（可能为 3D 场景、图表、表格等） | 根据插件定义的交互方式操作 |
| 数据加载中 | 加载进度指示器 | 等待或取消 |
| 计算中 | 计算进度指示器 | 等待或取消 |
| 错误状态 | 错误信息 + 重试按钮 | 重试或返回 |

**中央区域提供的容器能力**：

| 容器能力 | 说明 |
|:---|:---|
| Three.js 场景实例 | 提供 3D 场景、相机、光照、轨道控制器，供插件渲染 3D 内容 |
| 2D 画布容器 | 提供 Canvas 容器，供插件渲染 2D 内容 |
| 通用 DOM 容器 | 提供 div 容器，供插件渲染任意 DOM 内容 |
| 文件拖拽目标 | 支持拖拽文件到中央区域，自动检测文件格式并路由到对应插件 |

**3.2.4 右侧面板**

右侧面板显示当前激活插件的参数配置。

| 状态 | 显示内容 |
|:---|:---|
| 无插件激活 | 显示"请先激活一个插件" |
| 插件已激活 | 显示插件定义的参数控件：滑块、下拉菜单、数值输入框、复选框、文本输入框、文件选择按钮 |
| 参数变更 | 用户调整参数后，自动通知插件更新 |

**右侧面板提供的参数控件类型**：

| 控件类型 | 说明 |
|:---|:---|
| 滑块 (Range) | 数值型参数，带最小/最大值和步长 |
| 下拉菜单 (Select) | 枚举型参数 |
| 数值输入 (Number) | 精确数值输入 |
| 复选框 (Checkbox) | 布尔型参数 |
| 文本输入 (Text) | 字符串参数 |
| 文件选择 (File) | 数据文件选择按钮 |
| 按钮 (Button) | 触发操作（运行/停止/重置） |

**3.2.5 底栏**

| 区域 | 显示内容 |
|:---|:---|
| 左侧 | 状态文本（就绪 / 计算中 / 加载中 / 保存中 / 错误） |
| 中间 | GPU 信息（GPU 型号 + WebGPU 状态） |
| 右侧 | 当前激活插件名称、项目保存状态 |

**3.2.6 性能看板（悬浮窗）**

| 指标 | 说明 |
|:---|:---|
| FPS | 当前帧率 |
| 帧耗时 | 单帧渲染耗时 (ms) |
| GPU 计算耗时 | Compute Shader 执行耗时 (ms) |
| 显存占用 | 当前 GPU 显存使用量 (MB) |
| 当前数据规模 | 粒子数/节点数/体素数（由插件上报） |

性能看板可拖动位置，可收起/展开。


### 3.3 插件加载对话框功能

**3.3.1 标签切换**

| 标签 | 功能说明 |
|:---|:---|
| 插件市场 | 从配置的源拉取插件列表，展示可安装的插件 |
| 本地加载 | 用户选择本地 .cspkg 文件进行加载 |
| 内置示例 | 展示项目内置的示例插件 |

**3.3.2 插件列表**

| 功能 | 说明 |
|:---|:---|
| 搜索框 | 按插件名称搜索过滤 |
| 列表展示 | 显示插件名称、版本、作者、简短描述、状态标签 |
| 操作按钮 | 未安装显示 [安装]，已安装显示 [加载]，已加载显示 [卸载] |
| 详情区域 | 选中插件后显示详细描述、许可证、依赖信息 |

**3.3.3 已加载插件列表**

显示当前已加载的所有插件，每个显示名称和版本。


### 3.4 分享对话框功能

| 区域 | 功能说明 |
|:---|:---|
| 项目信息 | 显示当前项目名称和压缩后大小 |
| 包含内容 | 复选框：数据 / 参数设置 / 场景状态，用户选择要分享的内容 |
| 生成分享链接 | 序列化项目 → 压缩 → 生成短链接（使用 Gist 存储或 URL 编码） |
| 导出项目文件 | 生成 .clproj 文件下载 |
| 导出截图 | 截取当前 3D 场景，下载为 PNG |
| 链接框 + 复制按钮 | 显示生成的链接，点击复制到剪贴板 |


### 3.5 设置页功能

**3.5.1 通用设置**

| 设置项 | 选项 | 说明 |
|:---|:---|:---|
| 语言 | 中文 / English | 界面语言，保存到 localStorage |
| 主题 | 亮色 / 暗色 / 跟随系统 | 界面主题，保存到 localStorage |
| 自动保存间隔 | 30秒 / 60秒 / 120秒 / 关闭 | 自动保存频率 |

**3.5.2 GPU 设置**

| 设置项 | 选项 | 说明 |
|:---|:---|:---|
| WebGPU 后端 | 自动 / 强制 CPU 后备 | 控制是否强制使用 CPU 后备模式 |
| 显存限制 | 自动 / 512MB / 1GB / 2GB | 限制 GPU 显存使用量 |

**3.5.3 数据设置**

| 设置项 | 说明 |
|:---|:---|
| 缓存使用量 | 显示当前 IndexedDB 使用量 |
| 清理缓存 | 点击后弹出确认对话框，确认后清理所有缓存数据 |

**3.5.4 关于**

| 元素 | 说明 |
|:---|:---|
| 版本 | 显示当前版本号 |
| 许可证 | 显示许可证信息 |
| GitHub 链接 | 跳转到项目仓库 |
| 文档链接 | 跳转到项目文档 |

**3.5.5 返回按钮**

点击返回工作台，路由跳转到 /workbench。


## 四、项目管理功能

### 4.1 项目数据结构

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | UUID | 项目唯一标识 |
| name | string | 项目名称 |
| createdAt | timestamp | 创建时间 |
| updatedAt | timestamp | 最后更新时间 |
| data.files | FileEntry[] | 加载的文件列表 |
| data.processed | any | 处理后的内部数据 |
| state.activePlugin | string \| null | 当前激活的插件 ID |
| state.parameters | Record | 各插件的参数设置 |
| state.camera | CameraState | 3D 场景相机位置 |
| state.scene | SceneState | 场景状态快照 |
| metadata.version | string | 项目格式版本 |
| metadata.description | string \| null | 项目描述 |
| metadata.tags | string[] | 项目标签 |

### 4.2 项目操作功能

| 操作 | 触发方式 | 系统行为 |
|:---|:---|:---|
| 创建 | 点击"新建项目" | 生成 UUID，创建空项目，保存到 IndexedDB |
| 打开 | 点击"打开项目" | 读取 .clproj 文件，解析并恢复项目状态 |
| 保存 | 点击"保存"或 Ctrl+S | 序列化项目，写入 IndexedDB |
| 自动保存 | 定时触发 | 每隔设定时间自动保存，后台执行 |
| 另存为 | 点击"另存为" | 生成 .clproj 文件，浏览器下载 |
| 删除 | 在最近项目列表中点击删除 | 从 IndexedDB 移除项目 |
| 恢复 | 启动时自动检查 | 检查 IndexedDB 中是否有最近项目，自动恢复 |

### 4.3 项目存储方案

| 存储方式 | 用途 | 说明 |
|:---|:---|:---|
| IndexedDB | 项目主存储 | 存储全部项目数据，无大小限制 |
| localStorage | 用户偏好 | 存储语言/主题/GPU 设置等 |
| .clproj 文件 | 项目导出/备份 | 用户手动导出，包含完整项目数据 |


## 五、数据加载功能

### 5.1 加载方式

| 方式 | 用户操作 | 系统行为 |
|:---|:---|:---|
| 点击加载 | 点击中央区域的"选择文件"或顶栏的"加载数据" | 弹出文件选择对话框 → 用户选择文件 |
| 拖拽加载 | 从文件管理器拖拽文件到中央区域 | 拖拽时区域高亮 → 释放后检测文件 |
| 示例数据 | 点击中央区域的"示例数据" | 展示内置示例列表 → 用户选择加载 |
| 项目恢复 | 打开已保存项目 | 从 IndexedDB 自动恢复数据和参数 |

### 5.2 格式检测功能

| 检测方式 | 说明 |
|:---|:---|
| 扩展名检测 | 根据文件扩展名初步判断格式 |
| Magic Number 检测 | 读取文件头字节进行精确格式识别 |
| 多重格式支持 | 一个文件可能有多种有效格式，系统检测到全部匹配 |
| 未知格式处理 | 无法识别的格式，提示用户并列出已加载插件支持的格式列表 |

**插件如何声明支持的格式**：插件通过 `getSupportedFormats()` 方法返回其支持的文件格式列表（扩展名 + MIME 类型），系统根据此列表进行路由。

### 5.3 数据路由功能

| 路由逻辑 | 说明 |
|:---|:---|
| 精确匹配 | 文件格式精确匹配某插件的支持格式 → 路由到该插件 |
| 多插件匹配 | 多个插件支持同一格式 → 弹出选择对话框让用户选择 |
| 无匹配 | 无插件支持该格式 → 提示"当前已加载的插件不支持此格式" |
| 插件未激活 | 插件未激活但支持该格式 → 自动激活该插件 |


## 六、插件系统功能

### 6.1 插件生命周期管理

| 阶段 | 触发时机 | 系统行为 |
|:---|:---|:---|
| 发现 | 启动时扫描 | 扫描已安装插件列表，检测到新插件 |
| 安装 | 用户安装插件 | 下载/读取 .cspkg → 验证 manifest → 解压到 IndexedDB |
| 加载 | 用户点击加载 | 加载入口文件 → 调用 init → 注册到插件管理器 |
| 激活 | 用户点击插件图标 | 调用 activate → 插件接管中央区域 → 显示参数面板 |
| 停用 | 用户切换到其他插件 | 调用 deactivate → 释放资源 |
| 卸载 | 用户点击卸载 | 调用 destroy → 从 IndexedDB 移除插件包 |
| 更新 | 用户更新插件 | 下载新版本 → 卸载旧版本 → 安装新版本 |

### 6.2 插件管理器功能

| 功能 | 说明 |
|:---|:---|
| 插件注册表 | 维护所有已加载插件的注册信息 |
| 插件激活切换 | 管理当前激活的插件，激活时停用上一个 |
| 插件间通信 | 提供插件间的消息传递通道（发布/订阅模式） |
| 插件隔离 | 每个插件运行在独立的执行上下文，互不干扰 |
| 插件错误隔离 | 单个插件崩溃不影响其他插件和工作站核心 |

### 6.3 插件包功能

| 功能 | 说明 |
|:---|:---|
| 打包格式 | .cspkg = ZIP 压缩包 |
| 包结构 | manifest.json + dist/ + assets/ |
| 验证 | 加载时验证 manifest 完整性和签名（可选） |
| 存储 | 解压后存储在 IndexedDB 中 |

### 6.4 插件接口

**插件必须实现的方法**：

| 方法 | 说明 |
|:---|:---|
| init(api) | 初始化插件，接收平台 API |
| destroy() | 销毁插件，释放资源 |
| activate(context) | 激活插件，接管工作台 |
| deactivate() | 停用插件，释放临时资源 |
| render(container) | 渲染内容到指定容器 |
| updateParams(params) | 接收参数更新 |
| getParams() | 获取当前参数 |
| compute(input, onProgress) | 执行计算，返回结果 |

**插件可选实现的方法**：

| 方法 | 说明 |
|:---|:---|
| loadData(file) | 处理数据文件加载 |
| getSupportedFormats() | 返回支持的格式列表 |
| renderToScene(scene) | 渲染到 3D 场景 |
| onProjectSave() | 项目保存时的回调 |
| onProjectLoad() | 项目加载时的回调 |

### 6.5 插件市场功能

| 功能 | 说明 |
|:---|:---|
| 插件列表拉取 | 从配置的源（GitHub 仓库）拉取可用插件列表 |
| 插件搜索 | 按名称、作者、标签搜索 |
| 插件详情 | 显示名称、版本、作者、描述、评分、下载量 |
| 一键安装 | 点击安装按钮，自动下载并安装 |
| 插件更新 | 检测更新，一键更新 |
| 插件源配置 | 用户可添加/切换插件源 |


## 七、性能监控功能

### 7.1 采集指标

| 指标类型 | 具体指标 | 采集方式 |
|:---|:---|:---|
| 渲染性能 | FPS、帧耗时、渲染耗时 | 通过 requestAnimationFrame 采样 |
| GPU 性能 | Compute Shader 耗时 | 通过 GPU 时间戳查询（如果支持） |
| 显存 | 显存占用 | 通过 wgpu 的 Buffer 使用量统计 |
| 数据规模 | 粒子数/节点数/体素数 | 由插件主动上报 |
| 状态 | GPU 状态、插件状态 | 系统监控 |

### 7.2 展示方式

性能看板以悬浮窗形式显示在右下角，可拖动位置，可收起/展开。

### 7.3 告警功能

| 告警条件 | 触发行为 |
|:---|:---|
| FPS < 30 | 显示"低帧率"警告 |
| 显存占用 > 80% | 显示"显存不足"警告 |
| 计算耗时 > 50ms | 显示"计算耗时过长"警告 |


## 八、国际化功能

### 8.1 支持语言

| 语言 | 代码 | 状态 |
|:---|:---|:---|
| 简体中文 | zh-CN | 支持 |
| 英文 | en-US | 支持 |

### 8.2 语言检测

| 检测顺序 | 说明 |
|:---|:---|
| 1. localStorage | 检查用户之前保存的语言偏好 |
| 2. 浏览器语言 | 检测 `navigator.language` |
| 3. 默认 | 以上均无则使用 en-US |

### 8.3 切换功能

| 功能 | 说明 |
|:---|:---|
| 切换入口 | 顶栏语言切换下拉菜单 |
| 即时生效 | 切换后界面立即刷新，无需刷新页面 |
| 持久化 | 偏好保存到 localStorage |

### 8.4 翻译覆盖范围

| 范围 | 说明 |
|:---|:---|
| 系统界面 | 全部系统界面文本均支持双语 |
| 系统通知 | 全部通知/提示/错误信息均支持双语 |
| 插件接口 | 插件可通过 API 获取当前语言，自行翻译插件内容 |


## 九、主题功能

### 9.1 支持主题

| 主题 | 说明 |
|:---|:---|
| 亮色 | 白色背景，深色文字 |
| 暗色 | 深色背景，浅色文字 |

### 9.2 主题检测

| 检测顺序 | 说明 |
|:---|:---|
| 1. localStorage | 检查用户之前保存的主题偏好 |
| 2. 系统偏好 | 检测 `prefers-color-scheme` |
| 3. 默认 | 以上均无则使用亮色 |

### 9.3 切换功能

| 功能 | 说明 |
|:---|:---|
| 切换入口 | 顶栏主题切换下拉菜单：[亮色] [暗色] [跟随系统] |
| 即时生效 | 切换后界面立即刷新，无需刷新页面 |
| 持久化 | 偏好保存到 localStorage |

### 9.4 主题变量

所有界面颜色通过 CSS 变量控制，变量分为以下几类：

| 类别 | 说明 | 示例变量 |
|:---|:---|:---|
| 背景色 | 各级背景颜色 | --color-bg-primary, --color-bg-secondary |
| 文字色 | 各级文字颜色 | --color-text-primary, --color-text-secondary |
| 边框色 | 各级边框颜色 | --color-border, --color-border-hover |
| 强调色 | 强调/交互颜色 | --color-accent, --color-accent-hover |
| 功能色 | 成功/警告/错误 | --color-success, --color-warning, --color-error |
| 尺寸变量 | 间距/圆角/字体 | --radius-sm, --font-size-base |


## 十、部署功能

### 10.1 部署目标

GitHub Pages（纯静态托管）

### 10.2 部署流程

| 步骤 | 说明 |
|:---|:---|
| 1. 代码推送 | 开发者推送代码到 GitHub 仓库的 main 分支 |
| 2. 触发构建 | GitHub Actions 检测到推送，自动触发工作流 |
| 3. 构建 WASM | 使用 wasm-pack 编译 Rust 代码为 WASM |
| 4. 构建前端 | 使用 Vite 等工具构建前端资源 |
| 5. 部署 | 将构建产物部署到 gh-pages 分支 |
| 6. 访问 | 用户通过 `https://[username].github.io/[repo]/` 访问 |

### 10.3 访问地址

| 环境 | 地址 |
|:---|:---|
| 生产环境 | `https://[username].github.io/[repo]/` |
| 预览环境 | 可选 PR 预览部署 |


## 十一、错误处理功能

### 11.1 错误类型与处理方式

| 错误类型 | 处理方式 | 用户呈现 |
|:---|:---|:---|
| WebGPU 不可用 | 自动切换 CPU 后备模式 | 横幅提示，非阻塞 |
| WASM 加载失败 | 重试 3 次，间隔 1 秒 | 加载失败页面 + 重试按钮 |
| GPU 内存不足 | 自动降低精度或分块处理 | 提示 + 建议 |
| 插件加载失败 | 隔离失败插件，回退安全状态 | 错误对话框 + 详细信息 |
| IndexedDB 不可用 | 尝试内存保存，提示导出 | 警告横幅 |
| 文件格式不支持 | 显示支持格式列表 | 错误提示 |
| 数据过大 | 建议分块或使用示例数据 | 提示 + 建议 |

### 11.2 错误边界

| 边界 | 说明 |
|:---|:---|
| 应用级边界 | 捕获未处理的全局错误，显示降级界面 |
| 组件级边界 | 每个主要组件有独立的错误边界，单个组件崩溃不影响整体 |
| 插件级边界 | 插件崩溃不影响工作站核心功能 |


## 十二、国际化键值表

### 12.1 通用键

| 键 | 中文 | English |
|:---|:---|:---|
| common.cancel | 取消 | Cancel |
| common.confirm | 确定 | Confirm |
| common.save | 保存 | Save |
| common.load | 加载 | Load |
| common.delete | 删除 | Delete |
| common.close | 关闭 | Close |
| common.back | 返回 | Back |
| common.search | 搜索 | Search |
| common.ok | 确定 | OK |
| common.yes | 是 | Yes |
| common.no | 否 | No |

### 12.2 欢迎页键

| 键 | 中文 | English |
|:---|:---|:---|
| welcome.title | 欢迎使用 Ergalics Studio | Welcome to Ergalics Studio |
| welcome.enter | 进入工作台 | Enter Workbench |
| welcome.hardware.detecting | 检测硬件 | Detecting hardware |
| welcome.hardware.webgpu_available | WebGPU 可用 | WebGPU Available |
| welcome.hardware.webgpu_unavailable | WebGPU 不可用，将使用 CPU 后备模式 | WebGPU Unavailable, using CPU fallback |
| welcome.hardware.gpu | GPU | GPU |
| welcome.hardware.wasm_loaded | WASM 模块已加载 | WASM module loaded |
| welcome.hardware.storage_available | 存储可用 | Storage available |

### 12.3 工作台键

| 键 | 中文 | English |
|:---|:---|:---|
| workbench.sidebar.project | 项目 | Project |
| workbench.sidebar.plugins | 插件 | Plugins |
| workbench.sidebar.tools | 工具 | Tools |
| workbench.empty.title | 开始使用 Ergalics Studio | Start using Ergalics Studio |
| workbench.empty.subtitle | 选择以下方式开始工作 | Choose how to start |
| workbench.empty.load_plugin | 加载插件 | Load Plugin |
| workbench.empty.open_project | 打开项目 | Open Project |
| workbench.empty.drag_file | 拖拽文件到此处 | Drag file here |

### 12.4 插件键

| 键 | 中文 | English |
|:---|:---|:---|
| plugin.title | 加载插件 | Load Plugin |
| plugin.market | 插件市场 | Plugin Market |
| plugin.local | 本地加载 | Local Load |
| plugin.builtin | 内置示例 | Built-in Examples |
| plugin.install | 安装 | Install |
| plugin.uninstall | 卸载 | Uninstall |
| plugin.load | 加载 | Load |
| plugin.loaded | 已加载 | Loaded |
| plugin.search | 搜索插件 | Search plugins |
| plugin.no_plugins | 暂无插件 | No plugins available |

### 12.5 项目键

| 键 | 中文 | English |
|:---|:---|:---|
| project.new | 新建项目 | New Project |
| project.open | 打开项目 | Open Project |
| project.save | 保存 | Save |
| project.save_as | 另存为 | Save As |
| project.name | 项目名称 | Project Name |
| project.untitled | 未命名项目 | Untitled |
| project.auto_save | 自动保存中 | Auto-saving |
| project.saved | 已保存 | Saved |

### 12.6 设置键

| 键 | 中文 | English |
|:---|:---|:---|
| settings.title | 设置 | Settings |
| settings.general | 通用 | General |
| settings.language | 语言 | Language |
| settings.theme | 主题 | Theme |
| settings.theme_light | 亮色 | Light |
| settings.theme_dark | 暗色 | Dark |
| settings.theme_system | 跟随系统 | Follow System |
| settings.auto_save | 自动保存间隔 | Auto-save Interval |
| settings.gpu | GPU 设置 | GPU Settings |
| settings.gpu_backend | WebGPU 后端 | WebGPU Backend |
| settings.gpu_auto | 自动 | Auto |
| settings.gpu_cpu_fallback | 强制 CPU 后备 | Force CPU Fallback |
| settings.memory_limit | 显存限制 | Memory Limit |
| settings.memory_auto | 自动 | Auto |
| settings.data | 数据 | Data |
| settings.cache_usage | 缓存使用 | Cache Usage |
| settings.clear_cache | 清理缓存 | Clear Cache |
| settings.about | 关于 | About |
| settings.version | 版本 | Version |
| settings.license | 许可证 | License |
| settings.return | 返回工作台 | Return to Workbench |

### 12.7 分享键

| 键 | 中文 | English |
|:---|:---|:---|
| share.title | 分享项目 | Share Project |
| share.include | 包含内容 | Include |
| share.data | 数据 | Data |
| share.params | 参数设置 | Parameters |
| share.scene | 场景状态 | Scene State |
| share.generate_link | 生成分享链接 | Generate Share Link |
| share.export_file | 导出项目文件 | Export Project File |
| share.export_screenshot | 导出截图 | Export Screenshot |
| share.copy | 复制 | Copy |
| share.copied | 已复制 | Copied |

### 12.8 状态键

| 键 | 中文 | English |
|:---|:---|:---|
| status.ready | 就绪 | Ready |
| status.computing | 计算中 | Computing |
| status.paused | 已暂停 | Paused |
| status.loading | 加载中 | Loading |
| status.saving | 保存中 | Saving |
| status.error | 错误 | Error |


## 十三、开发任务清单

| 优先级 | 模块 | 核心任务 |
|:---|:---|:---|
| P0 | WASM 基础层 | Rust + wgpu 设备管理，WASM 导出接口，Compute Shader 调度框架 |
| P0 | 插件接口 | 完整类型定义，manifest 结构，生命周期接口 |
| P0 | 项目脚手架 | 目录结构、构建配置、TypeScript 配置 |
| P1 | 欢迎页 | 路由、硬件检测、语言/主题切换、进入工作台 |
| P1 | 工作台 | 四区域布局、空状态、顶栏/侧栏/底栏 |
| P1 | 插件管理器 | 加载/安装/激活/卸载、注册表、插件间通信 |
| P1 | 项目管理器 | IndexedDB 持久化、CRUD、导入导出 |
| P1 | 国际化 | i18n 配置、语言文件、切换组件 |
| P1 | 主题 | CSS 变量、主题切换、系统检测 |
| P2 | 数据加载 | 文件选择/拖拽、格式检测、路由 |
| P2 | 设置页 | 所有设置项、持久化 |
| P2 | 性能监控 | 指标采集、看板组件 |
| P2 | 分享功能 | 链接生成、导出项目、导出截图 |
| P3 | 插件市场 | 插件列表拉取、搜索、安装 |
| P3 | 错误处理 | 错误边界、降级策略、用户提示 |
| P3 | 部署流水线 | GitHub Actions、Pages 部署 |


## 十四、功能模块清单

| 模块 | 功能 | 状态 |
|:---|:---|:---|
| 欢迎页 | 硬件检测、语言/主题切换、进入工作台 | 待实现 |
| 工作台 | 四区域布局、空状态引导、文件拖拽 | 待实现 |
| 插件系统 | 加载/安装/激活/卸载、注册表、市场 | 待实现 |
| 项目管理 | 创建/保存/打开/导出/导入/自动保存 | 待实现 |
| 数据加载 | 文件选择/拖拽/格式检测/路由 | 待实现 |
| 国际化 | 中英双语、语言检测、切换 | 待实现 |
| 主题 | 明暗切换、系统跟随、CSS 变量 | 待实现 |
| 设置页 | 通用/GPU/数据/关于 | 待实现 |
| 性能监控 | 指标采集、看板、告警 | 待实现 |
| 分享 | 链接生成、导出项目、截图 | 待实现 |
| 错误处理 | 错误边界、降级、重试 | 待实现 |
| 部署 | GitHub Actions、Pages | 待实现 |