# Ergalics Studio 需求文档（PRD）

> 版本：v1.0（2026-09-18）
> 范围：V0.2 → V1.0 迭代路线中的全部功能需求与非功能需求
> 关联文档：[架构规范](./ARCHITECTURE.md)
> 状态：待评审

---

## 1. 需求背景

### 1.1 触发来源

- [Research-backed] 2026-09 对项目的综合评价显示：技术选型与行业主线高度合拍（WebGPU 1.0 于 2026 年 Q1 纳入 W3C 核心标准、Pyodide 0.26 发布、DuckDB-WASM 成熟），但存在三个明确短板——生态与协作（综合评分 4.5/10）、性能与规模（6/10）、AI 能力滞后于浏览器 AI 主线（ONNX Runtime Web / Transformers.js / WebLLM 已成为 2026 年浏览器推理的主流路径）。
- [PM judgment] 项目自身路线图存在两项未完成的承诺：完整 R 运行时、插件包签名与第三方安装管线。这两项是开放生态的前置条件，应优先收口。
- [PM judgment] 浏览器科学计算处于"标准刚落地、工具未定型"的窗口期：WebGPU 1.0 标准化、WASI 0.3.0 发布在即、JupyterLite 证明零安装路线可行。此时补齐生态与性能，比继续扩张功能面更符合项目利益。

### 1.2 当前痛点

- 科研用户完成一次分析后，把结果写成规范报告、图注、补充材料仍需大量手工劳动；投稿前缺少"要素检查"。
- R 语言用户打开代码模式会碰到语法降级提示（当前 R 运行在受限内置引擎上），体验与 Python 不一致。
- 数据超过一定规模后，导入与查询可能阻塞界面，用户对"浏览器能否处理真实科研数据"存疑。
- 分析过程与结果缺少"可引用"形态：没有软件引用条目、没有 DOI、没有可交付给期刊评审的复现快照。
- 新用户面对 14 个科研工具页与四种工作模式，缺少学科化的引导入口。
- 插件生态未开放：没有签名机制、没有第三方安装管线、没有稳定的开发者 API 承诺，第三方开发者无法放心投入。

---

## 2. 需求目标

### 2.1 定性目标

- 让"从打开页面到完成一次可复现分析并产出可投稿材料"成为一条无断点的主链路。
- 让 Ergalics Studio 成为浏览器端科研基础设施而非单纯工具：可被引用、可复现、可离线使用、可被第三方扩展。
- 保持"零安装、本地隐私优先、开源免费"的差异化定位，不与云 GPU 平台和商业 BI 正面竞争。

### 2.2 定量目标（本路线实施完成后）

| 指标 | 目标值 | 对应需求 |
|---|---|---|
| 新用户首次出图耗时 | ≤ 60 秒 | FR-01 |
| 常见检验一键成文 | ≥ 5 类（t/方差分析/卡方/相关/回归） | FR-02 |
| 第三方插件发布耗时 | ≤ 30 分钟 | FR-05、FR-22 |
| 500MB CSV 导入与查询期间界面帧率 | ≥ 30 FPS | FR-16 |
| 项目内 1GB 以上文件可打开并随机读取 | 支持 | FR-17 |
| 开源工程安全评分（Scorecards） | ≥ 7 分 | FR-18 |
| 可访问性（WCAG 2.1 AA）自测通过率 | ≥ 95% | FR-18 |
| 软件引用条目与 DOI | 每次 tagged release 自动产出 | FR-10 |
| 作品画廊收录 | ≥ 50 个作品 | FR-19 |
| 插件 SDK v1 兼容性 | 连续 3 个版本无破坏性变更 | FR-22 |

---

## 3. 竞争分析（摘要）

**结论**：Ergalics Studio 的生态位是"浏览器内多模式科研计算工作站"，市场上没有直接对标的完整产品；竞争压力来自五个方向，本路线以"差异化功能 + 科研合规 + 开放生态"应对，而非在他人主场硬碰。

| 竞争方向 | 代表产品 | 差异点 | 本路线的应对 |
|---|---|---|---|
| 浏览器 Notebook | JupyterLite | 仅 Notebook 形态 | 四模式共享 IR、科研工具矩阵（FR-04、FR-07） |
| 流程/可视化分析 | KNIME、Orange、Node-RED | 桌面为主或通用流式 | 浏览器原生 + 科研语义（FR-14、FR-15） |
| 云协作 Notebook | Colab、Deepnote、Hex | 云 GPU、团队协作 | 本地隐私 + 复现快照分享（FR-09、FR-19） |
| 可复现科研平台 | Quarto、Code Ocean、Binder | 容器化或文档输出 | 内置 repro.lock + 环境快照（FR-11） |
| 桌面科学计算 | MATLAB、Wolfram、Origin | 符号计算、工程仿真 | 开源免费 + 期刊流水线（FR-02、FR-03） |

---

## 4. 用户画像与使用场景

### 4.1 用户画像

| 画像 | 特征 | 核心诉求 | 频率 |
|---|---|---|---|
| 科研新手 | 本科/低年级研究生，会拖拽不太会编程 | 从模板快速起步、看懂每一步在算什么 | 高频 |
| 科研人员 | 有数据分析经验，需要产出论文材料 | 规范报告、投稿要素、可复现性、引用 | 高频 |
| 教师 | 用零安装环境授课 | 布置练习、检查学生提交、免部署 | 低频但单次时长长 |
| 数据分析爱好者 | 想尝试科学分析但不装环境 | 示例丰富、AI 辅助、可离线 | 中频 |
| 插件开发者 | 想扩展平台能力 | 稳定 API、签名发布、文档完善 | 低频 |

### 4.2 核心使用场景（按优先级）

1. 打开页面 → 选学科模板 → 拖入数据 → 出图 → 生成统计报告 → 导出投稿材料（新用户完整链路）。
2. 科研人员对已有项目做贝叶斯/信号/扫描分析 → 用运行记录对比 → 生成复现快照分享给合作者。
3. 教师创建课程 → 学生完成任务 → 教师批改 → 导出全班作业包。
4. 开发者按模板创建插件 → 本地签名 → 发布到市场 → 用户安装。
5. 断网/局域网环境（实验室、会议、飞机）下继续分析，离线可用。

---

## 5. 术语表

| 术语 | 定义 |
|---|---|
| .clproj | 项目文件格式，存储于 IndexedDB，含数据文件、运行记录、图表与配置 |
| repro.lock | 可复现锁文件：数据指纹 + 参数哈希 + 种子 + 代码快照 + 版本清单 |
| IR | 中间表示，流程/积木/代码三种模式共享的唯一事实来源 |
| cspkg | 插件包格式（manifest + 入口 + 资源的 ZIP） |
| ToolShell | 科研工具页的统一外壳（顶栏 + 主内容 + 状态栏） |
| WebGPU | W3C 标准化的浏览器 GPU 计算接口（2026 Q1 纳入核心标准） |
| WASM | WebAssembly，本项目 Rust 核心与 Python/R 运行时的编译目标 |
| OPFS | Origin Private File System，浏览器端大文件读写标准接口 |
| PWA | 渐进式 Web 应用，支持离线与安装 |
| SDK v1 | 插件开发者 API 的第一个稳定版本 |

---

## 6. 功能需求详述

> 按迭代期组织。每个功能条目包含：功能概述、业务逻辑（触发→处理→输出）、交互与页面要点、规则与边界。

### 6.1 V0.2 期：闭环收口

#### FR-01 学科模板库与场景化示例

**功能概述**：将现有"功能演示型"示例升级为"学科场景型"模板，让新用户从真实研究问题出发完成第一次分析。

**业务逻辑**：用户从欢迎页进入"从模板开始"，按学科浏览模板（物理实验误差分析、生物统计两样本比较、天文物证 FITS 处理、工程信号滤波等）。选择模板后，系统载入对应的示例项目与配套引导文案；引导文案逐步说明"这个项目在分析什么问题、每一步在算什么"。完成引导后，模板项目与普通项目无异，可继续编辑、保存、分享。

**交互与页面要点**：欢迎页在"新建/打开/示例"之外新增模板入口；模板卡片显示学科、难度、预计完成时间、涉及的科研工具；模板加载后右上角出现可关闭的引导浮层。

**规则与边界**：模板数量首期 ≥ 10 个学科；模板项目必须自带合法示例数据，不允许出现"数据缺失导致首屏空白"；引导文案中英文双语；模板改动不影响内置示例的完整性校验。

#### FR-02 统计报告自动撰写

**功能概述**：在分析页与模型实验室等科研工具中，将统计结果一键生成为符合学术规范的表述段落，可直接复制进论文。

**业务逻辑**：用户在完成一次检验或模型拟合后点击"生成结果段落"，系统依据结果与检验类型生成规范表述（如 `t(28) = 2.31, p = .028, Cohen's d = 0.85`），并附一句面向读者的解释（是否拒绝原假设、效应量大小）。生成内容可复制、可编辑，可插入报告生成器对应章节。

**交互与页面要点**：结果面板新增"生成表述"按钮；输出区提供"复制"与"插入报告"两个动作；支持中英文两种表述风格切换。

**规则与边界**：首期覆盖 t 检验、方差分析、卡方检验、相关分析、线性回归五类；p 值格式遵循期刊惯例（p < .001 显示为不等式）；对未达到显著性的结果给出中性表述，不诱导误读；效应量解释参照约定俗成的阈值并在文中注明。

#### FR-03 期刊投稿助手

**功能概述**：在 Figure Studio 与报告生成器中提供投稿要素检查与图注生成，减少投稿前的手工核对。

**业务逻辑**：用户在 Figure Studio 完成组图后，点击"投稿检查"：系统逐项核对导出设置（分辨率、字体嵌入、色彩模式、面板标注 a/b/c、图注文本）并给出通过/未通过清单；未通过项给出具体修改指引。图注生成基于图表类型、数据列名与统计结果自动起草，用户可编辑后定稿。

**交互与页面要点**：检查清单以分组列表呈现（图像质量/标注/文字/元数据），每项带"定位到问题处"跳转；图注生成器输出可预览、可编辑的文本块。

**规则与边界**：检查项与 IEEE/Elsevier 模板要求对齐；导出 PNG 必须携带分辨率元数据；图注默认语言跟随界面语言；检查结果不阻塞导出，但未通过项在导出时二次确认。

#### FR-04 完整 R 运行时

**功能概述**：将代码模式的 R 语言支持从"受限内置引擎"升级为"完整运行时"，与 Python 一致地运行自由语法与第三方包。

**业务逻辑**：用户在代码模式选择 R 标签后，默认进入完整 R 运行时；加载完成后可安装并使用标准 R 包（数据变换、绘图类）。`studio.*` API 以注入方式保留，使 R 与 Python/积木共享项目数据语义。运行中的程序可中断，中断后运行时重启，不卡死页面。

**交互与页面要点**：语言切换器显示引擎徽章（完整 R / 内置 IR），徽章说明当前缓冲区的执行引擎；首次加载完整运行时显示进度；控制台区分"完整 R 输出"与"DSL 降级提示"。

**规则与边界**：完整 R 运行时在独立 Worker 中执行，与页面隔离；安装包限制在内存与时间可承受范围内并给出提示；加载失败时回退到现有内置 IR 引擎并告知原因；三语言互译在 R 侧不再出现"跳过语法"的降级提示。

#### FR-05 插件签名与安装管线

**功能概述**：为 .cspkg 插件包引入签名与校验机制，并打通"打包→签名→发布→安装"的第三方管线。

**业务逻辑**：开发者用官方 CLI 工具对插件包签名（生成公钥指纹并写入 manifest）；用户在市场目录或本地安装插件包时，系统校验签名与指纹，通过后进入沙箱运行。安装记录与来源信息持久化到项目/应用存储，可在插件面板查看与卸载。

**交互与页面要点**：市场目录从"内置占位"升级为"可安装"；安装流程包含确认步骤（显示作者、指纹、权限范围）；签名校验失败时给出可读的错误信息与处理建议。

**规则与边界**：未签名包默认拒绝安装，提供显式的"信任此来源"二次确认；签名算法与密钥管理在架构规范中定义；安装数、卸载数纳入插件统计；恶意包不得绕过沙箱（沙箱隔离是独立的安全底线，签名不替代沙箱）。

#### FR-06 构建性能预算

**功能概述**：为构建产物建立可量化的性能预算，防止功能堆叠导致首屏劣化。

**业务逻辑**：构建时生成产物可视化报告；CI 对首屏 JS 体积（gzip）、科研路由 chunk 数量与懒加载完整性执行门槛检查，超限即失败并附体积增量报告。开发者在本地 `npm run build` 时获得同样的提示。

**交互与页面要点**：产出物为 CI 检查项 + 构建报告链接；不新增用户可见界面。

**规则与边界**：首屏 JS ≤ 500KB gzip（不含按路由懒加载的科研页）；新增依赖必须评估体积后才可合入；报告纳入历史趋势，防止回潮。

---

### 6.2 V0.3 期：生态开放与 AI 主线

#### FR-07 AI 分析助手

**功能概述**：用户用自然语言描述分析意图，系统生成可编辑、可运行的代码或流程，降低科研入门的编程门槛。

**业务逻辑**：用户在代码/积木模式旁的"AI 助手"面板输入自然语言（如"对 x 和 y 做相关分析并画散点图"），系统生成对应的 `studio.*` 代码或 IR 流程；生成结果以草稿形式呈现，用户可编辑后运行；运行报错时，助手可结合错误信息给出修正建议。在线模式可接入外部模型服务，离线模式使用浏览器本地模型（依赖 FR-08）。

**交互与页面要点**：面板内保留对话历史；生成代码插入当前缓冲区或流程画布；提供"重试""修改描述"操作。

**规则与边界**：首期覆盖 5 个典型意图模板（相关分析、回归、分布查看、检验、信号滤波）并保证可运行；生成代码必须调用受支持的 `studio.*` API，不允许生成随机 API；离线能力缺失时明确提示切换在线模式；对话内容不离开本地（在线模式需显式用户授权）。

#### FR-08 本地模型推理

**功能概述**：新增"模型推理"科研工具页，支持在浏览器内用 WebGPU 运行 ONNX 模型与预训练模型目录，结果落入项目数据与血缘。

**业务逻辑**：用户导入 ONNX 模型文件或从预训练目录选择模型，选择输入数据（文本或图像/表格），点击运行后系统在本地完成推理并展示结果；推理结果可保存为项目数据文件，自动接入血缘 DAG 与实验记录。

**交互与页面要点**：工具页遵循 ToolShell 统一外壳；运行前显示目标设备（WebGPU/CPU 回退）；推理进度与耗时可见；结果表格支持后续科研流程复用。

**规则与边界**：模型体积与内存限制给出提示；WebGPU 不可用时回退 CPU 并提示性能差异；推理设备与耗时写入运行记录，保证可复现；首期支持文本分类与图像分类各一个示例。

#### FR-09 可复现快照分享

**功能概述**：将"分享"升级为"可复现快照"：单个自包含 HTML 文件，内含数据指纹、repro.lock、结果图与复现指引，接收方无需安装即可核对与重跑。

**业务逻辑**：用户对项目发起"导出复现快照"，系统生成自包含 HTML（内联结果图与数据摘要、嵌入 repro.lock 与复现步骤）；接收方打开 HTML 即可查看复现状态与核对清单；快照文件可作为论文补充材料直接投递。

**交互与页面要点**：分享对话框提供"复现快照"与"原有分享"两种形态；快照导出过程显示进度；快照页内展示"复现检查"结果（数据指纹一致、参数一致等）。

**规则与边界**：快照不含原始大数据文件（仅指纹与摘要），避免体积失控；快照必须可离线打开；快照内嵌版本信息与生成时间；生成快照不修改原项目。

#### FR-10 软件引用与归档

**功能概述**：为项目提供标准软件引用元数据与 DOI 归档，满足期刊对软件引用的要求。

**业务逻辑**：发布 tagged release 时，CI 自动完成归档并产出 DOI；软件引用元数据（作者、版本、DOI、许可）在应用"关于"页展示，并提供一键复制引用条目的操作。

**交互与页面要点**：应用"关于/设置"页新增"引用本软件"区块；复制按钮提供 BibTeX 与纯文本两种格式。

**规则与边界**：引用信息必须与当前版本号一致；归档失败时 CI 显式报错而非静默跳过；许可与作者信息以仓库元数据为唯一来源，避免双份维护。

#### FR-11 可复现锁 v2

**功能概述**：扩展现有 repro.lock 的覆盖维度，加入环境快照与依赖指纹，并支持运行记录的结构化对比。

**业务逻辑**：项目保存时生成 v2 格式的 repro.lock，在原有数据/参数/种子/代码/版本之外增加运行时版本与依赖指纹；打开迁移或久置项目时执行漂移校验，新增"依赖漂移"检查项并给出建议。实验记录页支持选择两次运行做结构化 diff（参数、结果指标、配置差异），diff 结果可导出。

**交互与页面要点**：repro.lock 页面展示五类漂移的通过状态与详情；实验记录页的"对比"入口升级为可视化 diff 视图。

**规则与边界**：v2 格式兼容读取 v1 文件并提示升级；依赖指纹的采集范围在架构规范中定义；diff 仅对同项目内运行有效，跨项目对比不开放。

#### FR-12 GPU 内核扩展

**功能概述**：扩展 WebGPU 计算内核覆盖，让 GPU 加速惠及更多科研操作。

**业务逻辑**：新增 GPU 实现的内核：矩阵乘法、FFT、K-means、分箱聚合；对应 CPU 路径保留为回退。内核执行时上报耗时与内存占用，进入性能监控面板。

**交互与页面要点**：性能面板显示每个内核的 GPU 耗时与内存；数据规模达到 GPU 路径阈值时自动启用并标识。

**规则与边界**：GPU 路径必须与 CPU 路径结果一致（差异在数值容差内），由测试保障；无 WebGPU 设备时透明回退；内核实现与阈值配置集中在架构规范指定的目录。

---

### 6.3 V0.4 期：性能规模化与教学场景

#### FR-13 课程模式

**功能概述**：为教学场景提供"教师布置任务—学生完成—教师批改"的闭环，纯本地或局域网运行，不依赖云。

**业务逻辑**：教师创建课程并布置练习（引用模板、设定截止时间与可见性）；学生通过项目内的课程视图领取任务、完成分析；教师查看学生完成度（基于运行记录与 repro.lock 核对结果）并批改；教师可导出全班作业 ZIP（含每个学生的运行历史与结果快照）。

**交互与页面要点**：课程视图独立于科研工具列表，作为教学入口分组展示；批改视图按学生与任务组织；导出包内每个学生一个文件夹。

**规则与边界**：首期不引入账号体系（本地/局域网身份标识即可）；课程数据存于项目级存储；结果核验基于 repro.lock 而非代码执行，避免安全问题；导出包体积限制与压缩策略在架构规范中定义。

#### FR-14 数据清洗向导

**功能概述**：以可视化步骤流承载数据清洗全流程，每步即时预览影响，自动生成可重跑的清洗脚本。

**业务逻辑**：用户对数据文件启动清洗向导，按步骤执行类型转换、缺失值策略、离群值标记、去重、列重命名；每步实时预览影响行数与抽样结果；完成时系统生成等价的 `studio.*` 清洗脚本并保存为项目代码会话，可重跑。

**交互与页面要点**：向导为步骤条布局，支持前进/后退与任意跳步；每步提供"撤销此步"；脚本生成后展示预览并可手动修改。

**规则与边界**：清洗过程不直接改写源文件，产出新数据文件并记录血缘；步骤之间顺序可调整但存在依赖时给出提示；对 500MB 级文件，预览基于抽样且明确标注。

#### FR-15 3D 可视化增强

**功能概述**：扩展科研 3D 可视化能力：3D 表面图、体素渲染、百万级 GPU 点云。

**业务逻辑**：新增 3D 表面图与体素渲染插件，数据来自项目文件；点云渲染在 WebGPU 可用时支持百万级点并保持交互流畅；2D/3D 视图切换沿用现有"2D 激活时自动隐藏 3D 场景"的规则。

**交互与页面要点**：3D 插件沿用既有 Scene3DHandle 生命周期；视图控制（轨道、缩放、自动适配）保持一致。

**规则与边界**：50 万点以上点云帧率 ≥ 30 FPS（基准设备）；WebGPU 不可用时点云自动降采样并提示；3D 场景懒创建原则不变。

#### FR-16 Worker 池与流式数据通路

**功能概述**：将文件解析、哈希与重计算迁移到 Worker 池，配合流式查询，解决大文件阻塞主线程的问题。

**业务逻辑**：数据导入（解析、指纹、画像）在 Worker 池中执行；SQL 查询结果分页虚拟化渲染；数据规模超过阈值时自动降采样并提示；全程主线程保持响应。

**交互与页面要点**：导入过程中界面可继续操作，进度条来自 Worker 回传；查询结果表格虚拟滚动。

**规则与边界**：500MB CSV 导入与查询期间 FPS ≥ 30；Worker 数量与分片策略可配置且有默认值；内存超限时给出明确指引而非白屏。

#### FR-17 OPFS 存储升级

**功能概述**：将大文件分块存储从 IndexedDB 迁移到 OPFS，支持更大文件与随机读取。

**业务逻辑**：分块文件写入 OPFS（项目元数据仍存 IndexedDB），读取时按块懒加载；存储使用量可视化，提供"清理缓存"策略；旧 IndexedDB 存储数据可一键迁移。

**交互与页面要点**：设置页显示存储分布与清理入口；打开大文件时显示已加载比例。

**规则与边界**：项目内 1GB 以上文件可打开并随机读取分块；迁移过程可中断、可重试；OPFS 不可用的浏览器回退 IndexedDB。

#### FR-18 安全与可访问性合规

**功能概述**：达到开源工程安全评分与网页可访问性的行业基线。

**业务逻辑**：CI 集成工程安全评分、生成软件物料清单（SBOM）、执行依赖漏洞审计；应用通过键盘全程可操作、对比度达标、屏幕阅读器标注完整的自测。

**交互与页面要点**：无新增用户界面；安全与可访问性检查结果进入 CI 报告。

**规则与边界**：Scorecards ≥ 7 分；WCAG 2.1 AA 自测通过率 ≥ 95%；阻断级漏洞不允许合入。

---

### 6.4 V1.0 期：稳定发布

#### FR-19 作品画廊

**功能概述**：建立用户作品的展示与复用通道，形成传播与回流。

**业务逻辑**：用户将可复现快照（FR-09）附带元数据"分享到画廊"；画廊页按学科、图表类型、复现状态筛选展示作品；访客点击作品可查看快照或"在 Ergalics 中打开"。

**交互与页面要点**：画廊作为独立页面接入科研入口分组；分享流程与 FR-09 共用，附版权与许可选择。

**规则与边界**：画廊收录 ≥ 50 个作品后进入公开状态；作品可下架；公开内容不含原始私有数据。

#### FR-20 PWA 离线化

**功能概述**：应用支持安装为 PWA 并离线使用核心功能。

**业务逻辑**：构建时生成离线缓存清单（WASM、核心 chunk、示例数据）；用户安装 PWA 后，断网状态打开可访问示例项目与核心科研工具；提供"下载离线版"（单文件夹，双击即用）与局域网零配置启动脚本。

**交互与页面要点**：设置页提供安装引导与"下载离线版"入口。

**规则与边界**：断网下示例项目与核心功能可用；在线资源（如模型目录）明确标记需联网；离线版与在线版行为一致，不得出现功能阉割导致的困惑。

#### FR-21 主题市场

**功能概述**：开放主题包机制，丰富个性化与场景化外观。

**业务逻辑**：主题以 .cstheme 包形式分发（含配色、字体、密度配置），市场目录收录；用户安装后一键应用，全应用一致生效，含图表配色联动；提供科研风格（期刊双栏、衬线字体）与教育风格（高对比、大字号）官方主题。

**交互与页面要点**：设置页新增主题管理；主题预览提供即时切换效果。

**规则与边界**：主题不得包含可执行内容；与暗/亮模式正交兼容；图表配色联动为推荐性配置，缺省时回退默认。

#### FR-22 插件 SDK v1 稳定化

**功能概述**：冻结插件开发 API 为 v1，向第三方开发者提供兼容性承诺。

**业务逻辑**：发布 SDK v1 契约文档与弃用策略；提供脚手架与官方模板仓库；基于 v1 开发的插件在后续版本中无破坏性变更。

**交互与页面要点**：开发者文档页（文档站）提供 SDK 参考与迁移指南。

**规则与边界**：SDK v1 契约连续 3 个版本无破坏；新增能力以扩展而非修改既有 API 的方式演进；破坏性变更需提前一个版本发布弃用声明。

#### FR-23 性能基准体系

**功能概述**：建立可验证、可回归的性能基准。

**业务逻辑**：基准套件覆盖数据导入、GPU 内核、渲染帧率、内存占用四类关键路径；CI 对每次发布执行基准并与基线对比，无回归才允许合入；发布时产出基准报告页。

**交互与页面要点**：基准报告作为发布附件的网页呈现。

**规则与边界**：基准在固定基准设备与浏览器版本上执行并记录环境信息；波动允许范围 ±10%，超限视为回归。

#### FR-24 桌面封装

**功能概述**：以轻量桌面形态增强浏览器版本的能力边界（可选分支）。

**业务逻辑**：桌面封装复用全部 Web 资产，提供原生文件系统访问与更大内存窗口；Web 版与桌面版共用同一代码库。

**交互与页面要点**：桌面版可直接打开操作系统路径下的大文件；安装与更新流程遵循桌面平台惯例。

**规则与边界**：桌面封装不改变 Web 版行为；Web 版为默认形态，桌面为增强；桌面版引入的差异功能必须显式标注。

---

## 7. 非功能需求

### 7.1 性能

- 首屏 JS（gzip）≤ 500KB，科研路由按需懒加载。
- 常规操作反馈 ≤ 200ms；导入/查询类长任务必须给出进度与可取消能力。
- 500MB 级文件操作期间主线程帧率 ≥ 30 FPS。
- GPU 内核相对 CPU 路径在基准设备上 ≥ 3× 加速（矩阵乘法类目标 ≥ 5×）。

### 7.2 兼容性

- 支持四大现代浏览器的最新两个稳定版本：Chromium 系（Chrome/Edge）、Firefox、Safari。
- WebGPU/WASM 不可用时提供明确的能力降级与提示，功能不可静默缺失。
- 旧书签路由（科研工具旧路径）永久保持重定向兼容。

### 7.3 安全

- 第三方代码仅可在沙箱内运行，隔离边界不因签名机制而弱化。
- 依赖漏洞审计为合入门槛；SBOM 随发布生成。
- 用户数据默认不出本机；任何上传/在线行为需显式授权。

### 7.4 可访问性与国际化

- 键盘可完成全部主要操作；对比度与屏幕阅读器标注通过 WCAG 2.1 AA 自测。
- 中英文双语覆盖全部界面与新增文案；新功能未本地化前不得上线。

### 7.5 可复现性

- 所有运行类操作默认写入实验记录与血缘；导出物（报告、快照、图）携带版本与生成信息。
- repro.lock 覆盖数据、参数、种子、代码、版本、环境六维，任一漂移可检测。

### 7.6 可维护性

- 每个功能条目（FR-01 至 FR-24）必须伴随单元/E2E 测试，`npm run verify` 保持绿色。
- 新科研工具页遵循统一外壳与既有 UI 约束，不得引入新的页面范式。
- 依赖新增需过体积与必要性评估；核心模块保持纯 TS + 单测的边界。

---

## 8. 迭代排期

| 期 | 版本 | 功能 | 目标 |
|---|---|---|---|
| V0.2 | 0.2.x | FR-01 ~ FR-06 | 闭环收口：模板、报告、投稿、R 运行时、签名、预算 |
| V0.3 | 0.3.x | FR-07 ~ FR-12 | 生态开放与 AI 主线：AI 助手、推理、快照、引用、锁 v2、GPU 内核 |
| V0.4 | 0.4.x | FR-13 ~ FR-18 | 性能规模化与教学：课程、清洗、3D、Worker、OPFS、合规 |
| V1.0 | 1.0.x | FR-19 ~ FR-24 | 稳定发布：画廊、PWA、主题、SDK、基准、桌面 |
